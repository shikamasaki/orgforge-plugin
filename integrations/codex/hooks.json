{
  "//": "Codex projection of the org guardrails. Drop this at <repo>/.codex/hooks.json. It calls the SAME neutral adapter as the Claude Code plugin (integrations/common/org_hook.py) — Codex's PreToolUse hook shares Claude Code's exit-2 / permissionDecision:deny contract (verified 2026-07 against learn.chatgpt.com/docs/hooks), so one guardrail serves both harnesses. Set ORG_LEDGER_ROOT (and the ORG_CAP_* caps) in the environment Codex runs in. NOTE: a non-managed command hook must be trusted via /hooks, or run with --dangerously-bypass-hook-trust in unattended CI; prefer managed hooks for enforcement integrity.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Bash|Shell|Write|Edit|ApplyPatch)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CODEX_PROJECT_ROOT}/integrations/common/org_hook.py",
            "timeout": 30,
            "statusMessage": "org guardrail: checking blast-radius / mandate / doctrine"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CODEX_PROJECT_ROOT}/integrations/common/org_session_start.py",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
